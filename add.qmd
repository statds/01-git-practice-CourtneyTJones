Few
lines
of
text
adding a long line to the file so that it turns into a paragraph so i keep typing typing typing typing typing typing typing typing typing typing typing typing typing typing typing tyipng tyipng typing
now adding the short line